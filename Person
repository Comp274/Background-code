import java.io.*;

public class Person 
{
	public String Firstname, Lastname, id, password;
	public String age;
	public String gender;
	public String addressOne, addressTwo, city, state, zip;
	public String phone, email;
	public Boolean hasInsurance, isActive;
		
	public Person(String First, String Last, String ID, String Password,  String Phone, String Email) throws IOException
	{
		//File file = new File("C:\\Users\\D40067406\\Desktop\\" + ID + ".txt");
		//BufferedReader reader = new BufferedReader(new FileReader(file));
		//PrintWriter writer = new PrintWriter(new FileOutputStream(file, false));
		//if(file.exists() == true)
		/*{
			int index;
		
			String line = reader.readLine();
			while (line != null) 
			{

	            //look for substring in line
	            index = line.indexOf("1");
	            if(index >= 0 ) 
	            {
	                Firstname = (line.substring(index));
	            }
	            index = line.indexOf("2");
	            if(index >= 0 ) 
	            {
	                Lastname = (line.substring(index));
	            }
	            index = line.indexOf("3");
	            if(index >= 0 ) 
	            {
	                ID = (line.substring(index));
	            }
	            index = line.indexOf("4");
	            if(index >= 0 ) 
	            {
	                Password = (line.substring(index));
	            }
	            index = line.indexOf("5");
	            if(index >= 0 ) 
	            {
	                Phone = (line.substring(index));
	            }
	            index = line.indexOf("6");
	            if(index >= 0 ) 
	            {
	                Email = (line.substring(index));
	            }
			}

		}
		else
		{*/
		Firstname=First;
		//writer.write(Firstname + System.getProperty("line.separator"));
		Lastname=Last;
		//writer.write(Lastname + System.getProperty("line.separator"));
		id=ID;
		//writer.write(id + System.getProperty("line.separator"));
		password=Password;
		//writer.write(password + System.getProperty("line.separator"));
		phone=Phone;
		//writer.write(phone + System.getProperty("line.separator"));
		email=Email;
		//writer.write(email + System.getProperty("line.separator"));
		hasInsurance=false;
		isActive=true;
	//	}
	//	writer.close();
	//	reader.close();

	}

}
