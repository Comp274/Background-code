public class Teacher extends Person
{
	public String teachOne, teachTwo;
	public String department;
	
	public Teacher(String First, String Last, String ID, String Password, int Age, char Gender, String Phone,String Email)
	{
		super(First, Last, ID, Password, Age, Gender, Phone, Email);
		teachOne = "needs enrolled";
		teachTwo = "needs enrolled";
	}
	
	public String department ()
	{
		int random,teach;
		random = (int) (Math.random() * 3 + 1);
		if (random == 0)
		{
			department = "CA";
			return department;

		}
		else if (random == 1)
		{
			department = "SS";

			return department;
		}
		else
		{
			department = "AB";
			return department;
		}
	}
	public String Hours ()
	{
		int random;
		String officeHours;
		random = (int) (Math.random() * 5 + 1);
		
		if(random==1)
		{
			officeHours="Monday";
		}
		else if(random==2)
		{
			officeHours="Tuesday";
		}
		else if(random==3)
		{
			officeHours="Wednesday";
		}
		else if(random==4)
		{
			officeHours="Thursday";
		}
		else
		{
			officeHours="Friday";
		}
		return "Your office hours for this session are 11am to 4pm on " + officeHours + ".";
	}
}
